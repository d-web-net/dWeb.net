# Configure your Pages project
setup:
  git:
    # Ensure submodules are checked out
    clone: true
    submodules: true

build:
  command: hugo --minify
  output_dir: public
  environment:
    HUGO_VERSION: 0.92.2  # Use an older version that has the template 